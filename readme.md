# hiapp test

## how to install
run : 

`npm instal`

`ionic serve`

run with capacitor :

`ionic build`
`ionic cap add android` or `ionic cap add ios`

open with emulator :
`ionic cap open android` or  `ionic cap open ios`
